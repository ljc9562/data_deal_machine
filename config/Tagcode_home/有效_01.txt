#category:re
#author:jc
#cn_name:
#markdowm:作用
#辅助 包含[str.contains] ps(如果需要正则,增加参数regex = True)
#逻辑符号 1.和["&"]  2.且["|"]  3.非["~"]
#若输出是按列操作 需要加入frame[...]
#re表达式格式: ""

new_column_name = '区域'
columns_type = 'str'


#format
rule = \
[
#算有区域的地方
"地区哦->(frame['name'].str.contains('^周',regex = True))",

#计算大于700
"大于700哦->(frame['citycode']>=700)"
]

else_value = "真不知到"